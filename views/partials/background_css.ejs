<style>
     html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            background: #000; /* Dark background resembling night sky */
            color: white;
            font-family: Arial, sans-serif;
            overflow: hidden; /* Hide scrollbars */
        }

        body {
            overflow: auto;
            -ms-overflow-style: none;  /* Internet Explorer 10+ */
            scrollbar-width: none;  /* Firefox */
        }

        body::-webkit-scrollbar {
            display: none;  /* Safari and Chrome */
        }
    
    /* .background {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 40%;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 0;
        overflow: hidden; /* Prevent overflow due to animation 
    } */

    /* .twinkling-text {
        font-size: 100px;
        font-weight: bold;
        color: rgba(221, 211, 211, 0.974);
        position: absolute;
        /* animation: glow 1s infinite alternate, twinkle 1s infinite alternate; 
        animation: glow 3s infinite alternate;
    } */
    /* @keyframes glow {
        0% { text-shadow: 0 0 10px rgba(255, 255, 255, 0.5); }
        50% { text-shadow: 0 0 20px rgba(255, 255, 255, 1); }
        100% { text-shadow: 0 0 10px rgba(255, 255, 255, 0.5); }
    } */
    
    @keyframes twinkle {
        0% { opacity: 0.1; }
        100% { opacity: 1; }
    }
    
    .star {
        position: absolute;
        width: 3px;
        height: 3px;
        background: rgba(255, 255, 255, 0.8); /* White color with opacity */
        border-radius: 50%;
        animation: float 5s linear infinite, twinkle 2s infinite alternate;
        /* animation: glow 1s infinite alternate, twinkle 2s infinite alternate; */
    }
    @keyframes float {
        0% {
            transform: translateY(0) translateX(0);
        }
        50% {
            transform: translateY(-100px) translateX(50px);
        }
        100% {
            transform: translateY(0) translateX(-50px);
        }
    }

    
</style>


<body>
     <!-- <div class="background">
        <div class="twinkling-text">BLOGGO</div>
    </div> -->
</body>